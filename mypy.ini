[mypy]
# Only check the server package to avoid duplicate-path errors
files = server
ignore_missing_imports = True
python_version = 3.11
# Keep exclude for anything else that causes false positives
exclude = server/app/core/.*

[mypy-server.app.core.report_generator]
ignore_errors = True