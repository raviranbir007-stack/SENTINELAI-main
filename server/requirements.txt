# Core 
# Align pydantic-core with pydantic==2.12.5
pydantic-core==2.41.5
# Require modern pydantic compatible with google-genai
pydantic==2.12.5
pydantic-settings==2.1.0
# Allow pip to pick a FastAPI version compatible with anyio v4
fastapi>=0.118.0
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.45
# asyncpg is optional and can fail to build on some Python versions (e.g. 3.13);
# install it separately if you need Postgres async driver in a compatible environment.
# asyncpg==0.29.0
psycopg==3.2.1
alembic==1.12.1

# Redis & Celery
redis==5.0.1
celery==5.3.4
flower==2.0.1

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==41.0.7
bcrypt==4.1.2

# API Clients
aiohttp==3.13.2
# Use httpx compatible with google-genai
httpx==0.28.1
requests==2.31.0

# AI client: migrate to modern google-genai (preferred). Keep legacy fallback if needed.
google-genai==1.56.0
# legacy fallback (optional)
google-generativeai==0.3.0

# Report Generation
reportlab==4.0.9

# File Analysis
python-magic==0.4.27
pefile==2023.2.7
ssdeep==3.4
pypdf2==3.0.1
pillow==10.4.0

# AI/ML (optional - heavy; moved to `server/requirements-ml.txt`)
# scikit-learn==1.5.2
# numpy==2.3.5
# pandas==2.3.3
# joblib==1.3.2
# tensorflow==2.20.0
# torch==2.5.1

# Utilities
python-dotenv==1.0.0
email-validator==2.1.0

# Monitoring
prometheus-client==0.19.0

# Async sqlite driver (enable async SQLAlchemy with sqlite)
aiosqlite==0.20.0

# Async testing/runtime helpers
pytest-asyncio==0.21.0
# anyio v4 required by modern google-genai and newer httpx
anyio==4.12.0

# ============================================================
# ADDITIONS FOR GEMINI INTEGRATION
# ============================================================

# Enhanced Gemini Support
google-auth>=2.17.0
google-api-python-client>=2.105.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0

# Async and Retry Utilities
backoff>=2.2.0
coloredlogs>=15.0.0
structlog>=23.0.0
tenacity>=8.2.3

# Basic ML for Fallback Analysis (Lightweight)
numpy>=1.24.0
scipy>=1.11.0

# Advanced Logging
loguru>=0.7.0

# Configuration Management
pyyaml>=6.0.1
toml>=0.10.2

# Date/Time Handling
python-dateutil>=2.8.2
pytz>=2023.3

# Security Enhancements
pyjwt>=2.8.0
cryptography>=41.0.7

# Network Utilities
netifaces>=0.11.0
psutil>=5.9.0

# CLI Tools
click>=8.1.0
rich>=13.5.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

# Development Tools
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
isort>=5.12.0