# Optional heavy ML dependencies — install in separate virtualenv
tensorflow>=2.14.0
torch
scikit-learn
scipy
pandas
opencv-python
transformers
sentence-transformers# Optional ML / heavy packages
# Install only if you need on-device ML (may conflict with google-generativeai protobuf requirements)
scikit-learn==1.5.2
numpy==2.3.5
pandas==2.3.3
joblib==1.3.2
# TensorFlow and PyTorch are large and may have binary/compat issues on some platforms
tensorflow==2.20.0
torch==2.5.1
